#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('gen', '从一个模版中生成项目')
  .command('list', '列出所有模版')
  .parse(process.argv)
